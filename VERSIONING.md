# Versioning

The GRDN Ops Bot follows [Semantic Versioning 2.0.0](https://semver.org): `MAJOR.MINOR.PATCH`.

- While pre-1.0 (`0.y.z`), commands and behaviour may change between minor versions.
- The version lives in `package.json`. SemVer applies from `0.9.0` forward.
